# Normalization Definition

It is a technique often applied as data preparation during ML. The goal of nomalization is to change the values of numeric column in the datasets to use a common scale, without distorting differences in the range.